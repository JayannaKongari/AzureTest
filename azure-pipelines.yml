trigger:
  - master
pr:
  branches:
    include:
      - master

 

pool:
  vmImage: "ubuntu-latest"

 

variables:
  - group: secrets

 

stages:
  - stage: Test
    jobs:
      - job: codebeamer
        displayName: "codebeamer upload"
        steps:
          - script: |
              echo "Empty job."