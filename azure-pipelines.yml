trigger:
  - master
pr:
  branches:
    include:
      - master

 

pool:
  vmImage: "ubuntu-latest"

 

variables:
  - group: DevOps

 

stages:
  - stage: Test
    jobs:
      - job: codebeamer
        displayName: "codebeamer upload"
        steps:
          - script: |
              echo "Empty job."